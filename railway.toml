# Railway corre el worker Baileys, no Next.js (eso está en Vercel).
# Baileys exige Node 20; Nixpacks defaulted a 18.
[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "npx tsx worker/whatsapp.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
