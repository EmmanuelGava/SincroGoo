// Handler para normalizar mensajes entrantes de Email

export function normalizeEmailMessage(emailPayload: any) {
  // Extraer remitente, contenido, fecha, etc. del payload del servicio de email
  // Mapear al esquema de la base de datos (conversaciones)
  // Retornar el objeto normalizado
  return {};
} 